% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver-api.R
\name{hi_solver_get_primal_ray}
\alias{hi_solver_get_primal_ray}
\title{Get the Primal Ray (Unboundedness Certificate)}
\usage{
hi_solver_get_primal_ray(solver)
}
\arguments{
\item{solver}{An object of class "highs_solver".}
}
\value{
A list with `status` (integer, 0 = OK), `has_primal_ray` (logical), and
  `primal_ray` (numeric vector of length `n_col`, or `NULL` when no ray exists).
}
\description{
For a primal-unbounded LP (i.e. dual-infeasible), returns the primal unbounded
ray: a certificate of unboundedness, with one entry per variable (column).
}
\details{
As with the dual ray, this is a simplex-method certificate; requesting it may
solve an auxiliary LP.
}
